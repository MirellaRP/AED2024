// Basado en https://www.geeksforgeeks.org/cpp-program-to-implement-b-plus-tree/
#include <iostream>
#include <cmath>
#include <vector>
using namespace std;

template <typename T>
struct NodeBpT {
        NodeBpT* next;
        bool leaf;
        vector<T> llaves;
        vector<NodeBpT*> hijos;
};

template <typename T>
class BpT{
        NodeBpT<T>* root;
        int M;
        int k;


        public:
        BpT(int m): root(NULL), M(m), k(m-1){}

        bool find(NodeBpT<T>* node, T value){
                int i=0;

                if(node->leaf==false){  


                }




        }

        bool search(T value){
                return find(root, value);
        }





}



